#include <stdio.h>
#include "cordic-16bit.h"

//Print out sin(x) vs fp CORDIC sin(x)
int main(int argc, char **argv)
{
    double p;
    int s,c;
    int i; 
    int xx,aa,bb,cc;  
    cc = 0;
    bb = 0;
    aa = 0;

	for(aa=0; aa<50; aa++)


	xx = 0xa0d;
        cordic(xx, &s, &c, 16);
        printf("%d \n", s/MUL  );
       
}
